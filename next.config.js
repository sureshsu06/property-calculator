/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/property-calculator',
    images: {
      unoptimized: true,
    },
  }
  
  module.exports = nextConfig